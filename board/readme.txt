The Test Rig uses the F4 Discovery board, so these files are taken from stock ChibiOS. however pins need to be configured differently for PWM
output etc, so custom board files allow this to be done simply, and for the project to be ported to new hardware more easily.
